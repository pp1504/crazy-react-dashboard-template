import { createTheme } from "@mui/material";

export const fontFamilySecondary = "Almarai, Manrope, sans-serif";

const colorTextPrimary = "#323336";
const colorTextSecondary = "#676A75";

const theme = createTheme({
  components: {
  }
});

export default theme;
